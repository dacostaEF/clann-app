/**
 * Tema visual do Chat CLANN
 * Design premium inspirado em WhatsApp/Telegram
 */
export const chatTheme = {
  // Cores de fundo
  background: "#000000",
  backgroundGradient: ["#000000", "#0A0F24", "#000000"],
  
  // Bolhas de mensagem
  bubbleSent: "#3B82F6",        // Azul premium (mensagens enviadas)
  bubbleReceived: "#1E1E1E",     // Cinza escuro (mensagens recebidas)
  
  // Textos
  textPrimary: "#EAEAEA",        // Texto principal (branco suave)
  textSecondary: "#AFAFAF",      // Texto secundário
  textTertiary: "#7B7B7B",        // Texto terciário (timestamps)
  
  // Elementos de interface
  headerBackground: "#0A0F24",
  inputBackground: "#1A1A1A",
  inputBorder: "#2A2A2A",
  separatorColor: "#333333",
  
  // Status e indicadores
  onlineStatus: "#00FF9D",
  offlineStatus: "#7B7B7B",
  checkmarkRead: "#4FC3F7",
  checkmarkSent: "#AFAFAF",
  
  // Sombras
  shadowColor: "#000000",
  shadowOpacity: 0.3,
  shadowRadius: 8,
  
  // Espaçamentos
  padding: 16,
  paddingSmall: 8,
  paddingLarge: 24,
  
  // Bordas
  borderRadius: 18,
  borderRadiusSmall: 8,
  borderRadiusLarge: 24,
};

